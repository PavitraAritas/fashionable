import React from "react";

function Feed() {
  return (
    <div>
      Feeeeeeeeeeeed MEeeeeeeeeeeeeeeeeeeeeeee Use the element to define a link
      Use the href attribute to define the link address Use the target attribute
      to define where to open the linked document Use the element inside to use
      an image as a link Use the mailto: scheme inside the href attribute to
      create a link that opens the user's email program Use the element to
      define a link Use the href attribute to define the link address Use the
      target attribute to define where to open the linked document Use the
      element inside to use an image as a link Use the mailto: scheme inside the
      href attribute to create a link that opens the user's email program
    </div>
  );
}

export default Feed;
